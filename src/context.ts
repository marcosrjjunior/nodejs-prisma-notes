// import { createContext, ReactNode, useContext, useMemo } from 'react'

// const GlobalContext = createContext({} as any)

// interface GlobalProvider {
//   children: ReactNode
// }

// function GlobalProvider({ children }: GlobalProvider) {
//   const [state, setState] = useState()

//   return (
//     <GlobalContext.Provider value={state}>{children}</GlobalContext.Provider>
//   )
// }

// const useGlobal = (): GlobalContext => useContext(GlobalContext)

// export { GlobalContext, GlobalProvider, useGlobal }

// export default GlobalProvider
